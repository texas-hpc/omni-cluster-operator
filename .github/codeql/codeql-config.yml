name: omni-cluster-operator-codeql

disable-default-queries: false

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - api
  - cmd
  - internal

paths-ignore:
  - api/**/zz_generated.deepcopy.go
