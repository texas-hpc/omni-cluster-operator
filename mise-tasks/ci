#!/usr/bin/env bash
#MISE description="Run local CI verification tasks."
set -euo pipefail

mise run test
mise run lint-config
mise run lint
mise run build
mise run samples >/dev/null
mise run render-default >/dev/null
mise run chart-lint
mise run chart-template >/dev/null
