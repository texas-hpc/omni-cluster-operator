---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: omniclusteraddons.omni.texashpc.com
spec:
  group: omni.texashpc.com
  names:
    kind: OmniClusterAddon
    listKind: OmniClusterAddonList
    plural: omniclusteraddons
    singular: omniclusteraddon
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OmniClusterAddon is the Schema for the omniclusteraddons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OmniClusterAddon.
            properties:
              clusterRef:
                description: clusterRef attaches this addon to one OmniCluster in
                  the same namespace.
                properties:
                  name:
                    description: name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              helm:
                description: helm configures the chart to render into the Omni template.
                properties:
                  chart:
                    description: chart is the Helm chart name to render.
                    minLength: 1
                    type: string
                  namespace:
                    default: default
                    description: namespace is the Kubernetes namespace for rendered
                      objects.
                    minLength: 1
                    type: string
                  releaseName:
                    description: |-
                      releaseName is the Helm release name used while rendering.
                      Defaults to the OmniClusterAddon metadata.name.
                    minLength: 1
                    type: string
                  repository:
                    description: repository is the Helm repository URL that serves
                      the chart.
                    minLength: 1
                    type: string
                  values:
                    description: values is passed to Helm while rendering the chart.
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: version is the Helm chart version to render.
                    minLength: 1
                    type: string
                required:
                - chart
                - repository
                - version
                type: object
              manifestName:
                description: |-
                  manifestName is the Omni manifest sync entry name.
                  Defaults to the OmniClusterAddon metadata.name.
                minLength: 1
                type: string
              mode:
                default: full
                description: mode controls how Omni applies the rendered manifest.
                enum:
                - one-time
                - full
                type: string
            required:
            - clusterRef
            - helm
            type: object
          status:
            description: status defines the observed state of OmniClusterAddon.
            properties:
              chart:
                description: chart is the last Helm chart rendered by the controller.
                type: string
              chartVersion:
                description: chartVersion is the last Helm chart version rendered
                  by the controller.
                type: string
              clusterRef:
                description: clusterRef is the last cluster reference observed by
                  the controller.
                type: string
              conditions:
                description: conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastRenderTime:
                description: lastRenderTime is when the controller last rendered or
                  reused the cached manifest.
                format: date-time
                type: string
              manifestName:
                description: manifestName is the Omni manifest sync name rendered
                  by the controller.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation handled by
                  the controller.
                format: int64
                type: integer
              renderedManifestHash:
                description: renderedManifestHash is the SHA-256 hash of the cached
                  rendered manifest.
                type: string
              renderedManifestSecretRef:
                description: renderedManifestSecretRef names the Secret containing
                  the cached rendered manifest.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
