---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: omnihelmreleases.omni.texashpc.com
spec:
  group: omni.texashpc.com
  names:
    kind: OmniHelmRelease
    listKind: OmniHelmReleaseList
    plural: omnihelmreleases
    singular: omnihelmrelease
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OmniHelmRelease is the Schema for the omnihelmreleases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OmniHelmRelease.
            properties:
              atomic:
                description: atomic rolls back failed upgrades and uninstalls failed
                  installs.
                type: boolean
              chart:
                description: chart configures the Helm chart to install or upgrade.
                properties:
                  chart:
                    description: chart is the Helm chart name to reconcile.
                    minLength: 1
                    type: string
                  repository:
                    description: repository is the Helm repository URL that serves
                      the chart.
                    minLength: 1
                    type: string
                  values:
                    description: values is passed to Helm during install and upgrade.
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: version is the Helm chart version to reconcile.
                    minLength: 1
                    type: string
                required:
                - chart
                - repository
                - version
                type: object
              clusterRef:
                description: clusterRef attaches this release to one OmniCluster in
                  the same namespace.
                properties:
                  name:
                    description: name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              createNamespace:
                description: createNamespace asks Helm to create the release namespace
                  during install.
                type: boolean
              deletionPolicy:
                default: Uninstall
                description: deletionPolicy controls whether the workload-cluster
                  Helm release is uninstalled when this CR is deleted.
                enum:
                - Uninstall
                - Orphan
                type: string
              disableHooks:
                description: disableHooks prevents Helm hooks from running during
                  install, upgrade, and uninstall.
                type: boolean
              kubeconfigSecretRef:
                description: |-
                  kubeconfigSecretRef selects a Secret key containing a workload-cluster kubeconfig.
                  Create this explicitly, for example with OmniKubeconfigExport.
                properties:
                  key:
                    default: kubeconfig
                    description: key is the Secret data key. Defaults to kubeconfig.
                    minLength: 1
                    type: string
                  name:
                    description: name is the kubeconfig Secret name in the same namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              maxHistory:
                description: maxHistory limits the number of Helm release revisions
                  retained. Zero uses Helm's default.
                minimum: 0
                type: integer
              namespace:
                default: default
                description: namespace is the workload-cluster namespace for the Helm
                  release.
                minLength: 1
                type: string
              releaseName:
                description: releaseName is the Helm release name. Defaults to the
                  OmniHelmRelease metadata.name.
                minLength: 1
                type: string
              skipCRDs:
                description: skipCRDs skips CRDs on install. Helm does not upgrade
                  CRDs.
                type: boolean
              timeout:
                description: |-
                  timeout bounds Helm install, upgrade, wait, hook, rollback, and uninstall operations.
                  Defaults to 5m.
                type: string
              wait:
                description: |-
                  wait asks Helm to wait for reconciled resources to become ready.
                  Hooks are waited for even when wait is false.
                type: boolean
              waitForJobs:
                description: waitForJobs includes Jobs in Helm's wait behavior.
                type: boolean
            required:
            - chart
            - clusterRef
            - kubeconfigSecretRef
            type: object
          status:
            description: status defines the observed state of OmniHelmRelease.
            properties:
              chart:
                description: chart is the Helm chart name observed by the controller.
                type: string
              chartVersion:
                description: chartVersion is the Helm chart version observed by the
                  controller.
                type: string
              clusterRef:
                description: clusterRef is the last cluster reference observed by
                  the controller.
                type: string
              conditions:
                description: conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              kubeconfigSecretKey:
                description: kubeconfigSecretKey is the kubeconfig Secret key observed
                  by the controller.
                type: string
              kubeconfigSecretRef:
                description: kubeconfigSecretRef is the kubeconfig Secret name observed
                  by the controller.
                type: string
              lastAction:
                description: lastAction is the last Helm action attempted by the controller.
                type: string
              lastAttemptTime:
                description: lastAttemptTime is when the controller last attempted
                  a Helm action.
                format: date-time
                type: string
              lastError:
                description: lastError is the last Helm or credential error observed
                  by the controller.
                type: string
              lastSuccessTime:
                description: lastSuccessTime is when the controller last completed
                  a Helm action successfully.
                format: date-time
                type: string
              namespace:
                description: namespace is the workload-cluster namespace observed
                  by the controller.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation handled by
                  the controller.
                format: int64
                type: integer
              releaseName:
                description: releaseName is the Helm release name observed by the
                  controller.
                type: string
              releaseRevision:
                description: releaseRevision is the last Helm release revision observed
                  by the controller.
                format: int64
                type: integer
              releaseStatus:
                description: releaseStatus is the last Helm release status observed
                  by the controller.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
