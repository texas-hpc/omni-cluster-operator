---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: omnimachines.omni.texashpc.com
spec:
  group: omni.texashpc.com
  names:
    kind: OmniMachine
    listKind: OmniMachineList
    plural: omnimachines
    singular: omnimachine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OmniMachine is the Schema for the omnimachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OmniMachine
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: annotations are applied to the generated Omni resource.
                type: object
              clusterRef:
                description: clusterRef attaches this machine document to one OmniCluster
                  in the same namespace.
                properties:
                  name:
                    description: name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              install:
                description: install configures Talos installation for this static
                  machine.
                properties:
                  disk:
                    description: disk is the install disk path.
                    minLength: 1
                    type: string
                required:
                - disk
                type: object
              kernelArgs:
                description: kernelArgs are managed for this static machine.
                items:
                  type: string
                type: array
              labels:
                additionalProperties:
                  type: string
                description: labels are applied to the generated Omni resource.
                type: object
              locked:
                description: |-
                  locked prevents config updates, upgrades, and downgrades for this machine.
                  Omni allows locked machines only when used as workers.
                type: boolean
              machineID:
                description: machineID is the Omni machine ID. Defaults to metadata.name
                  when omitted.
                type: string
              patches:
                description: patches are applied to this machine.
                items:
                  description: Patch is a Talos machine configuration patch reference
                    used by Omni cluster templates.
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      description: annotations are applied to the generated config
                        patch.
                      type: object
                    file:
                      description: |-
                        file is a patch file path relative to the OmniCluster spec.templateRoot mounted in the operator.
                        Prefer inline for GitOps CRs unless the operator deployment mounts this file tree.
                      type: string
                    idOverride:
                      description: idOverride overrides Omni's generated config patch
                        ID.
                      type: string
                    inline:
                      description: inline is a Talos strategic machine configuration
                        patch.
                      x-kubernetes-preserve-unknown-fields: true
                    labels:
                      additionalProperties:
                        type: string
                      description: labels are applied to the generated config patch.
                      type: object
                    name:
                      description: name is the human-readable patch name.
                      type: string
                  type: object
                type: array
              systemExtensions:
                description: systemExtensions are installed on this machine.
                items:
                  type: string
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: status defines the observed state of OmniMachine
            properties:
              clusterRef:
                description: clusterRef is the last cluster reference observed by
                  the controller.
                type: string
              conditions:
                description: conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              machineID:
                description: machineID is the Omni machine ID rendered by the controller.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation handled by
                  the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
