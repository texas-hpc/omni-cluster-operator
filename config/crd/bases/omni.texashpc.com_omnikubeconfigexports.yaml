---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: omnikubeconfigexports.omni.texashpc.com
spec:
  group: omni.texashpc.com
  names:
    kind: OmniKubeconfigExport
    listKind: OmniKubeconfigExportList
    plural: omnikubeconfigexports
    singular: omnikubeconfigexport
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OmniKubeconfigExport is the Schema for managed workload-cluster
          kubeconfig exports.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OmniKubeconfigExport
            properties:
              allowClusterAdmin:
                description: allowClusterAdmin allows requesting the privileged system:masters
                  group.
                type: boolean
              clusterRef:
                description: clusterRef points to the workload OmniCluster in the
                  same namespace.
                properties:
                  name:
                    description: name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Delete
                description: deletionPolicy controls what happens to the target Secret
                  on resource deletion.
                enum:
                - Delete
                - Orphan
                type: string
              renewBefore:
                description: renewBefore requests rotation this long before expiration.
                type: string
              serviceAccount:
                description: serviceAccount configures a service-account kubeconfig
                  export.
                properties:
                  groups:
                    description: groups are the Kubernetes groups embedded in the
                      exported kubeconfig.
                    items:
                      type: string
                    minItems: 1
                    type: array
                  user:
                    description: user is the Kubernetes user subject embedded in the
                      exported kubeconfig.
                    minLength: 1
                    type: string
                required:
                - groups
                - user
                type: object
              targetSecretRef:
                description: targetSecretRef selects the Secret name to write kubeconfig
                  data into.
                properties:
                  name:
                    description: name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              ttl:
                description: ttl is the requested Omni service-account kubeconfig
                  validity period.
                type: string
            required:
            - clusterRef
            - serviceAccount
            - targetSecretRef
            - ttl
            type: object
          status:
            description: status defines the observed state of OmniKubeconfigExport
            properties:
              clusterRef:
                description: clusterRef is the last OmniCluster name observed by the
                  controller.
                type: string
              conditions:
                description: conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              expirationTime:
                description: expirationTime is when the currently exported kubeconfig
                  is expected to expire.
                format: date-time
                type: string
              kubeconfigHash:
                description: kubeconfigHash is the SHA-256 hash of the last exported
                  kubeconfig bytes.
                type: string
              lastRotationTime:
                description: lastRotationTime is when kubeconfig data was last requested
                  from Omni.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation handled by
                  the controller.
                format: int64
                type: integer
              targetSecretRef:
                description: targetSecretRef is the Secret name where kubeconfig data
                  is stored.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
